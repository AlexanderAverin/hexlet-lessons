const addDigits = (num) => {
    let n = String(num);
    let i = n.length;
    let result = null;
    let acc = null;
    while (i > 0) {
        i -= 1;
        acc = n[i];
        result += Number(acc);
    }
    const counter = (result) => {
        while (i > 0) {
            
        }
    }
};
console.log(addDigits(38));